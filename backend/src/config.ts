export const config = {
  // Fill these once the Move package is published and Pools object is created/shared.
  movePackageId: process.env.MOVE_PACKAGE_ID || "",
  poolsObjectId: process.env.POOLS_OBJECT_ID || ""
};
